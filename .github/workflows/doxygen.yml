name: Doxygen GitHub Pages Deploy Action

on:
  push:
    branches:
      - main

jobs:
  generate_doc:
    uses: mattnotmitt/doxygen-action@v1.9.5
    with:
      working-directory: '/'
      doxyfile-path: '/Doxyfile'
